# backend/api/urls.py

from django.urls import path
# from . import views # Import views when you create them

urlpatterns = [
    # Add API endpoints here later
    # e.g., path('products/', views.ProductList.as_view(), name='product-list'),
] 